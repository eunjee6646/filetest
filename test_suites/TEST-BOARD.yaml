metadata: 
    format: Lava-Test Definition
    name: TEST-BOARD
    devices:
    - v920-test1
    os:
    - debian
run:
  steps:
  - lava-test-case multinode-group --shell lava-group
  - lava-test-case bootlog --shell bootlog
  - lava-test-case enter-fast-boot --shell fast 0
  - lava-wait Rebooting 	
